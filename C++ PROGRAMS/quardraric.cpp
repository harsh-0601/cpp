#include <iostream>
using namespace std;
int main()
{
    int a, b, c, real, img, D;
    cout << "Enter numbers : ";
    cin >> a >> b >> c;
    D=b*b-(4*a*c);
    if(D>0)
    {
        
    }
    return 0;
}